import { Theme, Facts } from '@ds';

export const Cifras = () => (
  <Theme name="obscuro" style={{ padding: 32 }}>
    <Facts items={[['07', 'años'], ['02', 'disciplinas'], ['03', 'titulos'], ['01', 'estudio']]} />
  </Theme>
);
