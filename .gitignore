# OS
.DS_Store
Thumbs.db
desktop.ini

# Editor
.vscode/
.idea/
*.swp
*.swo
*~

# Node (just in case anyone adds tooling)
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnp.*

# Env
.env
.env.local
.env.*.local

# Build artifacts (we commit deploy/ on purpose — it's the bundled output)
dist/
build/

# Logs
*.log

# Project-internal state
.design-canvas.state.json
screenshots/
uploads/
scraps/

# design-sync: staged scripts, generated bundle, machine-local cache/state.
# El set durable (.design-sync/config.json, NOTES.md, conventions.md, previews/)
# SI se commitea — ver .design-sync/NOTES.md.
.ds-sync/
ds-bundle/
.design-sync/.cache/
.design-sync/learnings/
.design-sync/node_modules
